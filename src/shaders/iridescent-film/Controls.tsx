import {RotateCcw} from 'lucide-react';
import {defaults,type FilmParams} from './model';
export function Controls({params:p,set}:{params:FilmParams;set:(p:FilmParams)=>void}){
 const update=<K extends keyof FilmParams>(k:K,v:FilmParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof FilmParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Iridescent Film</span></div>{slider('strength','Film amount',0,100)}{slider('thickness','Film thickness',0,100)}{slider('bands','Color band frequency',0,100)}{slider('relief','Surface relief',0,100)}{slider('smoothness','Surface smoothing',0,20)}{slider('light','Light direction',-180,180)}{slider('gloss','Reflection strength',0,100)}{slider('saturation','Pearl saturation',0,100)}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
