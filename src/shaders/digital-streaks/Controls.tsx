import {RotateCcw} from 'lucide-react';
import {defaults,type StreakParams} from './model';
export function Controls({params:p,set}:{params:StreakParams;set:(p:StreakParams)=>void}){
 const update=<K extends keyof StreakParams>(k:K,v:StreakParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof StreakParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Digital Streaks</span></div>{slider('length','Streak length',0,300)}{slider('height','Slice height',1,20)}{slider('density','Fragment coverage',0,100)}{slider('dispersion','Color split',0,40)}{slider('angle','Streak angle',-90,90)}{slider('original','Original image',0,100)}{slider('seed','Pattern seed',1,100)}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
