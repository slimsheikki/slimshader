import {RotateCcw} from 'lucide-react';
import {defaults,type FlowParams} from './model';
export function Controls({params:p,set}:{params:FlowParams;set:(p:FlowParams)=>void}){
 const update=<K extends keyof FlowParams>(k:K,v:FlowParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof FlowParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Flow Trails</span></div>{slider('length','Trail length',0,300)}{slider('bend','Flow curvature',0,100)}{slider('direction','Flow direction',-180,180)}{slider('spacing','Strand spacing',4,24)}{slider('width','Strand width',1,6)}{slider('density','Strand density',0,100)}{slider('threshold','Shadow cutoff',0,100)}{slider('original','Original image',0,100)}{slider('seed','Pattern seed',1,100)}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
