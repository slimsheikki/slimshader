import {RotateCcw} from 'lucide-react';
import {defaults,type SpectralParams} from './model';
export function Controls({params:p,set}:{params:SpectralParams;set:(p:SpectralParams)=>void}){
 const update=<K extends keyof SpectralParams>(k:K,v:SpectralParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof SpectralParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Spectral Bloom</span></div>{slider('length','Ray length',0,100)}{slider('glow','Bloom strength',0,100)}{slider('dispersion','Color spread',0,100)}{slider('threshold','Highlight threshold',0,100)}{slider('centerX','Light center horizontal',0,100)}{slider('centerY','Light center vertical',0,100)}{slider('original','Original image',0,100)}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
