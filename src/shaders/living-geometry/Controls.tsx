import {RotateCcw} from 'lucide-react';
import {defaults,type GeometryParams} from './model';
export function Controls({params:p,set}:{params:GeometryParams;set:(p:GeometryParams)=>void}){
 const update=<K extends keyof GeometryParams>(k:K,v:GeometryParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof GeometryParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Living Geometry</span></div>{slider('motion','Movement strength',0,100)}{slider('duration','Loop duration · seconds',4,12)}{slider('boxes','Box count',0,8)}{slider('connections','Connecting lines',0,30)}{slider('particles','Particle amount',0,1000)}{slider('lineWidth','Line thickness',1,4)}{slider('opacity','Graphic opacity',0,100)}{slider('seed','Arrangement',1,100)}<label className="toggle-control"><span>Numerical labels</span><input type="checkbox" role="switch" checked={p.labels} onChange={e=>update('labels',e.target.checked)}/></label></section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
