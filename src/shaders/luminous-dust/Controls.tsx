import {RotateCcw} from 'lucide-react';
import {defaults,type DustParams} from './model';
export function Controls({params:p,set}:{params:DustParams;set:(p:DustParams)=>void}){
 const update=<K extends keyof DustParams>(k:K,v:DustParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof DustParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Luminous Dust</span></div>{slider('exposure','Exposure',-80,80)}{slider('contrast','Contrast',0,100)}{slider('cutoff','Shadow cutoff',0,80)}{slider('softness','Soft focus',0,20)}{slider('grain','Dust amount',0,100)}{slider('size','Grain size',1,6)}{slider('glow','Glow',0,100)}{slider('seed','Grain pattern',1,100)}<div className="color-row"><label><input aria-label="Light color" type="color" value={p.tint} onChange={e=>update('tint',e.target.value)}/><span>Light color</span></label></div></section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
