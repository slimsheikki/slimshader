export interface DustParams {exposure:number;contrast:number;cutoff:number;softness:number;grain:number;size:number;glow:number;tint:string;seed:number;transparent:boolean;background:string;}
export const defaults:DustParams={exposure:10,contrast:30,cutoff:12,softness:3,grain:72,size:1,glow:32,tint:'#c7e6ff',seed:17,transparent:false,background:'#020407'};
export {dimensions} from '../ascii/model';
