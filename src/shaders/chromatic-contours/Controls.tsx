import {RotateCcw} from 'lucide-react';
import {defaults,type ContourParams} from './model';
export function Controls({params:p,set}:{params:ContourParams;set:(p:ContourParams)=>void}){
 const update=<K extends keyof ContourParams>(k:K,v:ContourParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof ContourParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Chromatic Contours</span></div>{slider('strength','Contour strength',0,100)}{slider('threshold','Edge threshold',0,100)}{slider('width','Line softness',0,12)}{slider('dispersion','Color separation',0,40)}{slider('glow','Edge glow',0,100)}{slider('original','Original image',0,100)}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
