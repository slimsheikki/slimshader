import {RotateCcw} from 'lucide-react';
import {defaults,type GlassParams} from './model';
export function Controls({params:p,set}:{params:GlassParams;set:(p:GlassParams)=>void}){
 const update=<K extends keyof GlassParams>(k:K,v:GlassParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof GlassParams,label:string,min:number,max:number,suffix='%',step=1)=><label className="slider-control"><span>{label}<output>{p[k]}{suffix}</output></span><input aria-label={label} type="range" min={min} max={max} step={step} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Panels</span><span>01</span></div>{slider('count','Panel count',1,80,'')}{slider('width','Panel width',5,100)}<p className="control-hint">Width fills each panel slot. Lower it to leave clear gaps between the glass.</p>{slider('angle','Panel angle',-90,90,'°')}{slider('offset','Panel position',-100,100)}</section><section className="control-section"><div className="section-title"><span>Glass</span><span>02</span></div>{slider('refraction','Refraction',-100,150)}{slider('curve','Lens curvature',0,100)}{slider('distortion','Distortion',0,100)}{slider('blur','Frosted blur',0,20,' px',.5)}{slider('dispersion','Color dispersion',0,100)}{slider('highlights','Edge highlights',0,100)}{slider('edges','Edge shadows',0,100)}{slider('mix','Glass amount',0,100)}<p className="control-hint">Use a few wide panels for broad lenses, or many narrow panels for a rippled glass look.</p></section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Preserve transparency</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label>{!p.transparent&&<div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div>}</section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
