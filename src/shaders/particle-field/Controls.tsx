import {RotateCcw} from 'lucide-react';
import {defaults,type ParticleParams} from './model';
export function Controls({params:p,set}:{params:ParticleParams;set:(p:ParticleParams)=>void}){
 const update=<K extends keyof ParticleParams>(k:K,v:ParticleParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof ParticleParams,label:string,min:number,max:number)=><label className="slider-control"><span>{label}<output>{p[k]}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Particle Field</span></div>{slider('size','Particle size',1,8)}{slider('spacing','Particle spacing',2,16)}{slider('scatter','Scatter distance',0,100)}{slider('density','Particle density',0,100)}{slider('threshold','Shadow cutoff',0,100)}{slider('original','Original image',0,100)}{slider('seed','Pattern seed',1,100)}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label><div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
