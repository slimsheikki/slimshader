import {RotateCcw,ChevronDown} from 'lucide-react';
import {defaults,type HeatedParams} from './model';
export function Controls({params:p,set}:{params:HeatedParams;set:(p:HeatedParams)=>void}){
 const update=<K extends keyof HeatedParams>(key:K,value:HeatedParams[K])=>set({...p,[key]:value});
 const slider=(key:keyof HeatedParams,label:string,min:number,max:number,suffix='%')=><label className="slider-control"><span>{label}<output>{p[key]}{suffix}</output></span><input aria-label={label} type="range" min={min} max={max} value={Number(p[key])} onChange={e=>update(key,Number(e.target.value))} style={{'--fill':`${(Number(p[key])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Heat & light</span><span className="section-index">02</span></div>{slider('heat','Heat intensity',0,100)}{slider('rim','Hot rim width',0,100)}{slider('glow','Halo glow',0,100)}{slider('dispersion','Rainbow fringe',0,100)}{slider('light','Light direction',-180,180,'°')}</section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Transparent background</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label>{!p.transparent&&<div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span><code>{p.background}</code></label></div>}<p className="control-hint">Procedural forms with thermal color, soft halos, and spectral edges.</p></section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
