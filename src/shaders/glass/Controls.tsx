import {RotateCcw} from 'lucide-react';
import {defaults,splitPreset,type GlassParams} from './model';
export function Controls({params:p,set}:{params:GlassParams;set:(p:GlassParams)=>void}){
 const update=<K extends keyof GlassParams>(k:K,v:GlassParams[K])=>set({...p,[k]:v});
 const slider=(k:keyof GlassParams,label:string,min:number,max:number,suffix='%',step=1)=><label className="slider-control"><span>{label}<output>{p[k]}{suffix}</output></span><input aria-label={label} type="range" min={min} max={max} step={step} value={Number(p[k])} onChange={e=>update(k,Number(e.target.value))} style={{'--fill':`${(Number(p[k])-min)/(max-min)*100}%`} as React.CSSProperties}/></label>;
 return <div className="controls"><section className="control-section"><div className="section-title"><span>Starting point</span></div><div className="smudge-actions"><button className="subtle-button" onClick={()=>set({...defaults})}>Fine Flutes</button><button className="subtle-button" onClick={()=>set({...splitPreset})}>Split Glass</button></div></section><section className="control-section"><div className="section-title"><span>Panel placement</span></div><label className="field-label">Coverage<select aria-label="Coverage" value={p.placement} onChange={e=>update('placement',e.target.value as GlassParams['placement'])}><option value="full">Full image</option><option value="area">Selected area</option></select></label>{p.placement==='area'&&<>{slider('groupPosition','Group position',0,100)}{slider('groupWidth','Group width',5,100)}<p className="control-hint">Enable placement handles above the image to move and resize the glass group.</p></>}</section><section className="control-section"><div className="section-title"><span>Panels</span><span>01</span></div>{slider('count','Panel count',1,80,'')}{slider('width','Panel width',5,100)}<p className="control-hint">Width fills each panel slot. Lower it to leave clear gaps between the glass.</p>{slider('angle','Panel angle',-90,90,'°')}{slider('offset','Panel position',-100,100)}</section><section className="control-section"><div className="section-title"><span>Glass</span><span>02</span></div>{slider('distance','Subject distance',0,100)}{slider('thickness','Glass thickness',0,100)}{slider('focus','Focus on glass',0,100)}<p className="control-hint">Low focus keeps the subject crisp. Higher focus sharpens the glass and softens the subject behind it.</p>{slider('refraction','Refraction',-100,150)}{slider('curve','Lens curvature',0,100)}{slider('distortion','Distortion',0,100)}{slider('blur','Frosted blur',0,20,' px',.5)}{slider('dispersion','Color dispersion',0,100)}{slider('reflections','Surface reflections',0,100)}{slider('light','Light direction',-90,90,'°')}{slider('highlights','Edge highlights',0,100)}{slider('edges','Edge shadows',0,100)}{slider('mix','Glass amount',0,100)}<p className="control-hint">Use a few wide panels for broad lenses, or many narrow panels for fine optical flutes.</p></section><section className="control-section"><div className="section-title"><span>Canvas</span></div><label className="toggle-control"><span>Preserve transparency</span><input type="checkbox" role="switch" checked={p.transparent} onChange={e=>update('transparent',e.target.checked)}/></label>{!p.transparent&&<div className="color-row"><label><input aria-label="Background color" type="color" value={p.background} onChange={e=>update('background',e.target.value)}/><span>Background</span></label></div>}</section><button className="reset-button" onClick={()=>set({...defaults})}><RotateCcw size={14}/> Reset all settings</button></div>;
}
