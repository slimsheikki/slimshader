import {test,expect} from '@playwright/test';
for(const [name,control,value] of [['Flow Trails','Flow curvature','95'],['Iridescent Film','Film thickness','90'],['Spectral Bloom','Ray length','80'],['Particle Field','Scatter distance','60'],['Chromatic Contours','Color separation','20'],['Digital Streaks','Streak length','240']])test(`${name} changes pixels and exports transparent high-resolution PNG`,async({page})=>{
 await page.goto('/');await page.getByRole('button',{name:`Open ${name} editor`,exact:true}).click();
 const png=await page.evaluate(()=>{const c=document.createElement('canvas');c.width=240;c.height=180;const x=c.getContext('2d')!;const g=x.createLinearGradient(0,0,240,180);g.addColorStop(0,'#ff5522');g.addColorStop(1,'#44bbff');x.fillStyle=g;x.fillRect(70,50,100,80);x.fillStyle='white';x.fillRect(90,60,4,30);return c.toDataURL().split(',')[1];});await page.getByLabel('Upload image').setInputFiles({name:'test.png',mimeType:'image/png',buffer:Buffer.from(png,'base64')});
 const c=page.getByLabel(`${name} image preview`);await expect.poll(()=>c.evaluate((c:HTMLCanvasElement)=>c.width)).toBe(240);const first=await c.evaluate((c:HTMLCanvasElement)=>c.toDataURL());await page.getByRole('slider',{name:control,exact:true}).fill(value);await expect.poll(()=>c.evaluate((c:HTMLCanvasElement)=>c.toDataURL())).not.toBe(first);
 await page.getByRole('switch',{name:'Transparent background'}).check();await expect.poll(()=>c.evaluate((c:HTMLCanvasElement)=>c.getContext('2d')!.getImageData(0,0,1,1).data[3])).toBe(0);
 await page.getByLabel('Export scale').selectOption('2');const promise=page.waitForEvent('download');await page.getByRole('button',{name:/Export PNG/}).click();const d=await promise;const fs=await import('node:fs/promises');const buf=await fs.readFile((await d.path())!);expect(buf.readUInt32BE(16)).toBe(480);expect(buf.readUInt32BE(20)).toBe(360);
});
